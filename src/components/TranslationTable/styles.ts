import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  headerText: {textAlign: 'center', width: '100%'},
  table: {zIndex: -100, marginBottom: 50},
});

export default styles;
